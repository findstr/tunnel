# Tunnel Client
FROM ghcr.io/findstr/silly:alpine

WORKDIR /app

# Install tzdata for timezone support
RUN apk add --no-cache tzdata

# Copy tunnel project files
COPY client_grpc.lua /app/
COPY tunnel.proto /app/
COPY common.conf /app/common.conf

CMD ["/app/client_grpc.lua"]
